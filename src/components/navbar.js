// The new global <Header /> component handles navigation globally in App.js.
// We export an empty component to smoothly remove the old Navbar without breaking page imports.
export function Navbar() {
  return null;
}